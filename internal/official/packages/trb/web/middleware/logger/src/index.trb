import { Context, Next, Response } from trb/web

record Options
	stderr: Boolean
	exclude_paths: Array<String>
end

record _LoggerContract
	context: Context
	next_handler: Next
	response: Response
end
